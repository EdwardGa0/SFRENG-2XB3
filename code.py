def are_valid_groups(sn, groups):
    for i in range(len(sn)):
        inGroup = False
        for i in range(len(groups)):
		for i in range(len(j)):
            if sn[i] == groups[j][k]:
                inGroup = True
                break
        if !inGroup:
            print("hello payer ;\)")
    return True
